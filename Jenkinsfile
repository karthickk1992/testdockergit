pipeline {
  agent { dockerfile true } // Use Dockerfile as the agent for the pipeline

 stages {
        stage('Hello') {
            steps {
                echo 'Hello World'
            }
        }
    }
}